import React from "react";
import LoginPage from "./LoginPage";

export const metadata = { title: "Login Page: Work Manager" };

const login = () => {
  return <LoginPage />;
};

export default login;
